<%= simple_form_for([ recipe, transaction ]) do |f| %>
  <%= f.input :comment %>
  <%= f.input :rating , collection: [1,2,3,4,5] %>
  <%= f.button :submit, value: "Leave my review", class: "btn btn-warning btn-lg"%>
<% end %>
